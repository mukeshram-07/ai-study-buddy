# System Architecture

The AI Study Buddy uses:
- Streamlit for UI
- Groq LLM for content generation
- Pexels API for image retrieval

User input flows through the UI, is processed by the LLM, and rendered as structured outputs.
